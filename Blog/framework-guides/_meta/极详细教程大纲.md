# 极详细教程 · 章节骨架（124 篇复用）

复制下列标题到各篇 `index.md`，按栈删减（无 `public/` 则删「七」；无 Swagger 则在「八」说明）。

---

## frontmatter（YAML）

```yaml
---
title: "{Stack} 极详细教程：在 Framework 仓库里跑通 …"
series: framework
category: {Back-end|Front-end|Full-stack|Tooling}
stack: {Stack}
language: {Python|TypeScript|…}
repo_path: {repo_path}
source: {source_rel}
github: {github}
status: published
date: YYYY-MM-DD
tags: [...]
---
```

## 正文骨架

### 写在前面

- 为什么学、读完能做什么（ bullet 3～5 条）
- 前置知识、预计耗时（如 60～90 分钟）
- 仓库路径、GitHub 链接

### 目录

- 锚点列表（链接到下方各章）

### 一、{技术} 是什么？

- 比源文档 `框架简介` 深 2～3 倍
- 与相近栈对比表（2～4 行）

### 二、本仓库子工程目录树

- `tree` 或手写列表
- **文件职责表**（路径 | 作用）

### 三、环境准备

- 语言/运行时版本（Python 3.11+、Node 20+ 等）
- Windows PowerShell 注意点（执行策略、`-LiteralPath`）

### 四、从零跑通（步骤 1～N，N≥10）

每步格式：

```markdown
#### 步骤 K：{标题}

**目的**：…

```powershell
# 命令
```

**你应该看到**：…
```

必含：进入目录 → 安装依赖 → 启动 → 浏览器验证 → API 验证（PowerShell）→ Swagger（若有）

### 五、依赖文件精读

- 完整粘贴 `requirements.txt` / `package.json`
- 每个主要依赖一行说明

### 六、入口文件精读

- 完整粘贴 `main.py` / `main.ts` / `app.module.ts` 等
- 分小节：imports、中间件/模块、每个路由/handler

### 七、静态页 / 前端入口精读（若有）

- 完整或分段粘贴 `public/index.html`、`app/page.tsx` 等
- 说明浏览器如何请求 API

### 八、OpenAPI / Framework 约定

- `/api/health`、`/api/info` 或本栈等价路径
- 预期 JSON 字段表

### 九、动手扩展（非仓库自带）

- 小练习完整代码 + 如何撤销
- 明确不写回上游 Framework

### 十、与同仓栈对照

- 2～3 篇站内 `/blog/...` 链接 + 对照维度

### 十一、常见问题（≥6 条）

- 端口、venv、CORS、路径、Node 版本、依赖装错目录…

### 十二、小结与延伸阅读

- takeaway 4～6 条
- 官方文档、系列索引
